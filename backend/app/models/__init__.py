from app.core.database import Base
from app.models.user import User
from app.models.document import Document

# This file is used to import all models so that Base.metadata.create_all works correctly
